# Frontend Environment Variables Example
# Copy to .env and update as needed

# GraphQL HTTP endpoint (Apollo Client)
VITE_GRAPHQL_HTTP_URL=http://localhost:3000/graphql

# GraphQL WebSocket endpoint for subscriptions (graphql-ws protocol)
VITE_GRAPHQL_WS_URL=ws://localhost:3000/graphql