{
  "name": "shared-patterns",
  "sourceRoot": "shared/patterns/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p shared/patterns/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/shared/patterns"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p shared/patterns/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for shared/patterns yet\""
      }
    }
  },
  "tags": [
    "scope:shared",
    "type:lib"
  ]
}
