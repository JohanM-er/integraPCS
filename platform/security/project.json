{
  "name": "platform-security",
  "sourceRoot": "platform/security/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p platform/security/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/platform/security"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/security/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/security yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
