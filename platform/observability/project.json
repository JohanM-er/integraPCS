{
  "name": "platform-observability",
  "sourceRoot": "platform/observability/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p platform/observability/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/platform/observability"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/observability/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/observability yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
