{
  "name": "platform-config",
  "sourceRoot": "platform/config/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p platform/config/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/platform/config"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/config/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/config yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
