{
  "name": "platform-auth",
  "sourceRoot": "platform/auth/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p platform/auth/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/platform/auth"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/auth/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/auth yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
