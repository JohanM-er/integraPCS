{
  "name": "platform-db",
  "sourceRoot": "platform/db/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc -p platform/db/tsconfig.json"
      },
      "outputs": ["{workspaceRoot}/dist/platform/db"]
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/db/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/db yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
