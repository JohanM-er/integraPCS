{
  "name": "platform-db",
  "sourceRoot": "platform/db/src",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "options": {
        "main": "platform/db/src/index.ts",
        "outputPath": "dist/platform/db",
        "tsConfig": "platform/db/tsconfig.json"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p platform/db/tsconfig.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No specific lint config for platform/db yet\""
      }
    }
  },
  "tags": [
    "scope:platform",
    "type:lib"
  ]
}
